apiVersion: "appid.cloud.ibm.com/v1"
kind: OidcPolicy
metadata:
  name: {{.Name}}
spec:
  oidcClientName: {{.Spec.ClientName}}
  target:
  {{ range .Spec.Target }}
  - serviceName: {{.ServiceName}}
    paths:
    {{ range .Paths }}
    - {{.}}
    {{ end }}
  {{ end }}