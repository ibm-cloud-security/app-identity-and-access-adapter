apiVersion: "appid.cloud.ibm.com/v1"
kind: JwtPolicy
metadata:
  name: {{.Name}}
spec:
  jwksUrl: {{.Spec.JwksURL}}

  target:
  {{ range .Spec.Target }}
    - serviceName: {{.ServiceName}}
      paths:
      {{ range .Paths }}
        - {{.}}
      {{ end }}
  {{ end }}
    